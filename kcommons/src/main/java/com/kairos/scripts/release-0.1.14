
ACTIVITY

db.time_Type.update({"label" : "Unpaid break"},{$set:{"breakPaymentSetting":"UNPAID"}})
db.time_Type.update({"label" : "Paid Break"},{$set:{"breakPaymentSetting":"PAID"}})

// To create Constraints on country level
db.glideTimeSettings.createIndex( { "countryId": 1 }, { unique: true } )